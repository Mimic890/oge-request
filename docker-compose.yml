services:
  oge-bot:
    build: .
    env_file:
      - .env
    environment:
      - HTTP_PROXY=${HTTP_PROXY:-}
      - HTTPS_PROXY=${HTTPS_PROXY:-}
      - NO_PROXY=${NO_PROXY:-}
      - TZ=${TZ:-UTC}
    restart: unless-stopped
    volumes:
      - ./data:/app/data
